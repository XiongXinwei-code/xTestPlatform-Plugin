<UserControl x:Class="NiDaq.UI.Views.NiDaqAiReadEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             xmlns:expr="clr-namespace:ExpressionTextBox;assembly=ExpressionTextBox"
             syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=Windows11Light}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid Margin="16" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="任务名称" VerticalAlignment="Center"/>
        <expr:ExpressionTextBox Grid.Row="0" Grid.Column="1" ScriptText="{Binding TaskName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ExpectedResultType="System.String" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="读取样本数" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SamplesToRead, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="变量前缀" VerticalAlignment="Center"/>
        <expr:ExpressionTextBox Grid.Row="2" Grid.Column="1" ScriptText="{Binding ResultVariablePrefix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ExpectedResultType="System.String" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="3" Grid.Column="0" Text="导出格式" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="3" Grid.Column="1" SelectedItem="{Binding ExportFormat}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="4" Grid.Column="0" Text="输出目录" VerticalAlignment="Center"/>
        <expr:ExpressionTextBox Grid.Row="4" Grid.Column="1" ScriptText="{Binding OutputDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ExpectedResultType="System.String"/>
    </Grid>
</UserControl>
