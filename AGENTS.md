# 项目协作规则

## 提交规范

- 使用 Conventional Commits 格式：`type(scope): 简短摘要`。
- 提交标题中的简短摘要必须使用中文，类型和作用域保持 Conventional Commits 英文格式。
- 每个提交都必须有正文，依次说明：
  - `原因：` 修复的问题或变更动机；
  - `改动：` 修改的关键行为与兼容性影响；
  - `验证：` 已执行的构建、测试或未验证的部分。
- 涉及协议、硬件或固件兼容性的修改，正文必须写明受影响的报文/接口和回退策略。

## 变更验证

- 修改插件核心或 UI 后，至少构建受影响的项目及其 UI 项目（若存在）。
- 无法进行硬件实测时，在提交正文和交付说明中明确标注。
