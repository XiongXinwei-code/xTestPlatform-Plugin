<UserControl x:Class="SerialPortPlugin.UI.Views.SerialPortReadEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=Windows11Light}"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <syncfusion:TabControlExt CloseButtonType="Hide" AllowDragDrop="False" EnableLabelEdit="False">
        <syncfusion:TabItemExt Header="SerialPort Read">
            <Grid Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="端口名:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PortName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1" Text="目标变量:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding TargetVariable, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="2" Text="读取模式:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <ComboBox Grid.Row="2" Grid.Column="1" Text="{Binding ReadMode}" IsEditable="False" Margin="0,0,0,8">
                    <ComboBoxItem Content="Line" IsSelected="True"/>
                    <ComboBoxItem Content="Bytes"/>
                    <ComboBoxItem Content="Until"/>
                </ComboBox>

                <TextBlock Grid.Row="3" Text="字节数:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ByteCount, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="4" Text="终止符:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Terminator, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="5" Text="编码:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <ComboBox Grid.Row="5" Grid.Column="1" Text="{Binding Encoding}" IsEditable="False" Margin="0,0,0,8">
                    <ComboBoxItem Content="ASCII" IsSelected="True"/>
                    <ComboBoxItem Content="UTF8"/>
                    <ComboBoxItem Content="Hex"/>
                </ComboBox>

                <TextBlock Grid.Row="6" Text="超时 (ms):" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding TimeoutMs, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
            </Grid>
        </syncfusion:TabItemExt>
    </syncfusion:TabControlExt>
</UserControl>
