<UserControl x:Class="SerialPortPlugin.UI.Views.SerialPortWriteEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=Windows11Light}"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="350" d:DesignWidth="800">
    <syncfusion:TabControlExt CloseButtonType="Hide" AllowDragDrop="False" EnableLabelEdit="False">
        <syncfusion:TabItemExt Header="SerialPort Write">
            <Grid Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="端口名:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PortName, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1" Text="发送数据:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Data, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="False" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="2" Text="编码:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <ComboBox Grid.Row="2" Grid.Column="1" Text="{Binding Encoding}" IsEditable="False" Width="200" HorizontalAlignment="Left" Margin="0,0,0,8">
                    <ComboBoxItem Content="ASCII" IsSelected="True"/>
                    <ComboBoxItem Content="UTF8"/>
                    <ComboBoxItem Content="Hex"/>
                </ComboBox>

                <TextBlock Grid.Row="3" Text="追加换行:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding AppendNewLine}" Margin="0,0,0,8"/>
            </Grid>
        </syncfusion:TabItemExt>
    </syncfusion:TabControlExt>
</UserControl>
