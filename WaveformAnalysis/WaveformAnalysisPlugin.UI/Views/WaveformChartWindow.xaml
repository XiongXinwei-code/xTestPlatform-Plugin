<Window x:Class="WaveformAnalysisPlugin.UI.Views.WaveformChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:skinManager="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
        skinManager:SfSkinManager.Theme="{skinManager:SkinManagerExtension ThemeName=Windows11Light}"
        Title="波形分析结果" Width="900" Height="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <syncfusion:SfChart x:Name="Chart" Grid.Row="0">
            <syncfusion:SfChart.PrimaryAxis>
                <syncfusion:NumericalAxis x:Name="XAxis"/>
            </syncfusion:SfChart.PrimaryAxis>
            <syncfusion:SfChart.SecondaryAxis>
                <syncfusion:NumericalAxis Header="幅值"/>
            </syncfusion:SfChart.SecondaryAxis>
        </syncfusion:SfChart>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <TextBlock x:Name="TxtInfo" VerticalAlignment="Center" Margin="0,0,16,0" Foreground="Gray"/>
            <Button Content="关闭" Padding="16,4" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
