<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
			 xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
			 xmlns:expr="clr-namespace:ExpressionTextBox;assembly=ExpressionTextBox"
			 syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=Windows11Light}"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
			 d:DesignHeight="400" d:DesignWidth="800"
			 x:Class="Http.UI.Views.HttpJsonExtractEditorView">
	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVis" />
	</UserControl.Resources>
	<syncfusion:TabControlExt CloseButtonType="Hide" AllowDragDrop="False" EnableLabelEdit="False">
		<syncfusion:TabItemExt Header="Http JsonExtract"
							   Image="pack://application:,,,/Http.StepPlugin.UI;component/Resources/Icons/http.png"
							   ImageHeight="20" ImageWidth="20">
			<ScrollViewer VerticalScrollBarVisibility="Auto">
				<Grid Margin="16">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="160" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
					<TextBlock Grid.Row="0" Grid.Column="0" Text="JSON 源:" VerticalAlignment="Center" Margin="0,6" />
					<expr:ExpressionTextBox Grid.Row="0" Grid.Column="1" Margin="0,6"
						ScriptText="{Binding SourceJson, Mode=TwoWay}"
						SequenceFile="{Binding SequenceFile, RelativeSource={RelativeSource AncestorType=UserControl}}"
						EditPosition="{Binding EditPosition, RelativeSource={RelativeSource AncestorType=UserControl}}" />
					<TextBlock Grid.Row="1" Grid.Column="0" Text="未命中判失败:" VerticalAlignment="Center" Margin="0,6" />
					<CheckBox Grid.Row="1" Grid.Column="1" Margin="0,6" VerticalAlignment="Center"
							  IsChecked="{Binding FailOnMissingPath}" />
					<TextBlock Grid.Row="2" Grid.Column="0" Text="提取映射:" VerticalAlignment="Top" Margin="0,10" />
					<DataGrid Grid.Row="2" Grid.Column="1" Margin="0,6" Height="200"
							  ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="True">
						<DataGrid.Columns>
							<DataGridTextColumn Header="JSON 路径" Width="*" Binding="{Binding Path, UpdateSourceTrigger=PropertyChanged}" />
							<DataGridTextColumn Header="目标变量" Width="200" Binding="{Binding TargetVariable, UpdateSourceTrigger=PropertyChanged}" />
							<DataGridTextColumn Header="默认值" Width="120" Binding="{Binding DefaultValue, UpdateSourceTrigger=PropertyChanged}" />
						</DataGrid.Columns>
					</DataGrid>
                </Grid>
			</ScrollViewer>
		</syncfusion:TabItemExt>
	</syncfusion:TabControlExt>
</UserControl>