# UI 求值/预热入口清单

## 当前改造状态（替代下方历史审计结论）

历史审计中“校验不执行脚本”“UI 没有求值入口”的表述不成立，现明确撤销：
`ValidateExpression` 会编译并加载脚本，`TryEvaluateAsync` 会实际求值；间接调用同样会在调用进程产生脚本程序集。

当前源码路径：

| 入口 | 当前路径 | 边界 |
| --- | --- | --- |
| 平台工程调试 | DebugDocumentContext → RemoteEngineSession → EngineHostSession → LocalEngineSession | 已移除主程序本地执行分支和 UseRemoteEngineHost 开关，不自动回退 |
| 文件校验 / Copilot 消费的文件结果 | RemoteSequenceFileValidationService → ValidationHostSession → ValidationCore | 使用未保存的序列快照和工程全局变量 |
| ExpressionTextBox 语法检查 | IExpressionValidationService → 同一校验宿主 → ExpressionValidation | UI 不创建 RuntimeContext 或默认求值器 |
| ExpressionTextBox 手动检查 | 同上，Evaluate=true | 宿主实际求值，沿用原类型匹配规则；ExpectedResultType 仍由 XAML 指定 |
| UI 编辑器预热 | 创建控件及绑定模型 | 表达式控件构造不再获取默认求值器；外部插件构造副作用另行审计 |
| Core / 非 WPF 使用者 | 保留本地引擎能力 | 不代表允许平台主程序回退到本地执行 |

主程序为表达式控件配置 `ExpressionValidationServiceProvider.Service`，共享现有远程文件校验单例，退出时清除引用并释放校验宿主。
其他使用编辑器 SDK 的宿主也必须注入该服务；没有配置时明确提示不可用，不自行在 WPF 进程求值。

同时修复了远程初始化丢失插件目录、首次加载前未创建引擎、入口失败消息被吞掉，以及握手没有响应超时等问题。
普通 Build 和 Publish 均配置了 EngineHost / ValidationHost 子目录产物；实际发布和安装测试仍需执行。
正常关闭先请求停止、等待确认，执行进程句柄释放不再以断连或超时为由强杀进程。

### 尚未完成的整体验收与后续项

- P6：事件发送仍使用异步投递，消息背压、完整重连快照、会话代际、命令去重及历史条目/字节预算尚未完成统一验证，不能宣称通信和历史已全部有界。
- UI 部分同步会话调用及事件回调的线程模型仍需专项回归，尤其弹窗、关闭中断连和晚到响应。
- 远程状态/步骤结果投影完整性、子实例查询、工程重载和断点恢复需要完整无硬件兼容性测试。
- 校验请求取消目前仅取消 UI 等待，不保证立即中断宿主内表达式；禁止把独立进程视为安全沙箱。
- 停止确认来自现有引擎停止流程，不代表已经实测设备资源清理或所有并发任务收敛。
- P7：全新进程脚本模块审计、长时对象保留、故障矩阵、完整发布安装、SDK 包以及现场设备兼容性均未验证。
- 外部插件和独立 Demo 本轮未改造；插件程序集在 UI 中仍会用于元数据和序列化，构造与静态初始化必须无硬件副作用。

以下内容为迁移前的历史快照，仅保留溯源，不作为当前完成度或安全性依据。

---

## 历史 P0 审计（已被上方修正）

> 状态：仅审计现有代码，未修改任何源码。
> 目的：核对《执行引擎进程隔离与SDK分层改造方案.md》P0 阶段要求——摸清 UI 直接调用求值/预热入口、插件执行与初始化依赖的现状，为后续引擎进程隔离提供依据。
> 审计范围：`xTestPlatform.slnx` 内全部项目（含 `Demo/` 下独立示例项目，单独标注）。
> 审计方法：全文检索 `new RuntimeContext(`、`new SequenceRunner(`、`RoslynScriptExecutor`、`TestMath`、`ExpressionEvaluatorFactory`、`EnsureWarmedUp`、`PrecompileProjectExpressions`、`SequenceLoader` 等关键符号，逐一查看命中处上下文并分类。

## 一、结论摘要

- 当前 UI 层（WPF 主程序、`xTestPlatform.Controls`、`ProjectEditor`、`StepEditor/*`、`xTestCopilot` 等）**没有**直接引用 `RoslynScriptExecutor` 类型，也没有直接调用 `TestMath` 中的函数。
- UI 层对 `ExpressionEvaluatorFactory.Default` 的调用仅出现在两处，且均为**语法/类型校验用途**，不涉及真实脚本执行：
  - `SequenceFileValidation/Services/SequenceFileValidationService.cs`（文件级静态校验）
  - `StepEditor/ExpressionTextBox/ExpressionTextBoxViewModel.cs`（编辑器内联校验）
- 真正的“求值预热”入口 `ExpressionEvaluatorFactory.EnsureWarmedUp()` 与 `SequenceRunner.PrecompileProjectExpressions(...)` 只在 `xTestPlatform.Core` 引擎内部（`RuntimeContext.cs`、`SequenceExecutor.cs`、`SequenceRunner.cs`）被调用，UI 不直接触发。
- `SequenceLoader.Load(...)` 是纯反序列化（读取 `.xseq` 文件生成 `SequenceFile` 内存模型），**不触发 Roslyn 编译或求值**；UI 层多处直接调用属于“读取序列数据用于展示/校验/AI 工具生成”场景，不计入求值/预热耦合范畴，但因涉及直接 new/静态调用引擎侧类型，仍在此列出供后续 SDK 分层参考。
- `new RuntimeContext(` 与 `new SequenceRunner(` 的直接实例化，在主解决方案内仅出现在验证服务与引擎内部封装（`LocalEngineSession`）中；唯一的“UI 直接 new SequenceRunner”命中位于 `Demo/` 独立示例项目，不属于 `xTestPlatform.slnx` 迁移范围。

## 二、详细清单

### 2.1 `RuntimeContext` 直接实例化

| 文件 | 行号 | 调用方 | 用途分类 | 说明 |
|---|---|---|---|---|
| `SequenceFileValidation/Services/SequenceFileValidationService.cs` | 133 | 文件级校验服务 | 仅校验 | 为校验单个序列创建独立 `RuntimeContext`，加载序列并逐步校验；不执行任何设备操作或真实脚本运行，生命周期随 `using` 结束即释放 |
| `xTestPlatform.Core/Engine/SequenceRunner.cs` | 202-203 | 引擎内部 | 引擎内部 | `SequenceRunner` 自身创建/管理 `RuntimeContext`，非 UI 直接调用 |
| `Tests/**` | 多处 | 单元测试 | 测试 | 不影响生产 UI 耦合评估 |

**结论**：主解决方案中唯一“UI 侧可见”的 `RuntimeContext` 直接实例化点是校验服务，且明确是隔离的一次性校验上下文，不构成执行期耦合。

### 2.2 `SequenceRunner` 直接实例化

| 文件 | 行号 | 调用方 | 用途分类 | 说明 |
|---|---|---|---|---|
| `xTestPlatform.Core/Engine/Sessions/LocalEngineSession.cs` | - | `IEngineSession` 实现 | 引擎/会话桥接 | 属于已完成的 P3 会话抽象层，UI 通过 `IEngineSession` 间接使用，不算作直接耦合 |
| `Demo/xTestUserInterfaceDemo/xTestUesrInterfaceDemo/xTestUserInterface/MainWindow.xaml.cs` | 22 | 独立示例项目 | 遗留直接耦合（非主方案范围） | `var runner = new SequenceRunner(new WpfMessagePopupService());`；该项目不在 `xTestPlatform.slnx` 内，作为历史示例保留，若未来清理遗留耦合模式需一并处理 |

**结论**：主解决方案内 UI 不再直接 `new SequenceRunner(...)`，均通过 `IEngineSession`/`LocalEngineSession` 间接访问，符合 P3 阶段验收标准 G6/G7 方向。`Demo` 项目属于范围外遗留代码。

### 2.3 `RoslynScriptExecutor` / `TestMath` 直接引用

| 文件 | 命中内容 | 分类 | 说明 |
|---|---|---|---|
| `xTestPlatform.Core/Services/ExpressionEngine/RoslynScriptExecutor.cs` | 类型定义、`using static TestMath` | 引擎内部 | 核心实现文件本身 |
| `xTestPlatform.Core/Engine/SequenceRunner.cs` | `RoslynScriptExecutor.ClearCacheStatic()`（清缓存）、`RoslynScriptExecutor.PrecompileAll(...)`（预编译） | 引擎内部 | 引擎生命周期管理调用，非 UI |
| `Tests/xTestPlatform.Core.Tests/TestMathWaveformBufferTests.cs` | `using static TestMath` | 测试 | 仅测试项目直接使用 `TestMath`，与 UI 无关 |

**结论**：全仓库范围内未发现任何 UI 层项目直接引用 `RoslynScriptExecutor` 类型或直接调用 `TestMath` 静态函数。

### 2.4 `ExpressionEvaluatorFactory` / `EnsureWarmedUp`

| 文件 | 行号 | 调用方 | 用途分类 | 说明 |
|---|---|---|---|---|
| `SequenceFileValidation/Services/SequenceFileValidationService.cs` | 65 | 校验服务 | 仅校验 | `_expressionEvaluator = expressionEvaluator ?? ExpressionEvaluatorFactory.Default;` 用于静态校验表达式语法，不执行脚本副作用 |
| `StepEditor/ExpressionTextBox/ExpressionTextBoxViewModel.cs` | 109 | 编辑器控件 ViewModel | 仅校验 | 编辑期语法/类型校验，供 `ExpressionTextBox` 控件即时反馈，不涉及真实设备/序列执行 |
| `xTestPlatform.Core/Engine/RuntimeContext.cs` | 468 | 引擎内部 | 预热（引擎内部触发） | `_ = ExpressionEvaluatorFactory.EnsureWarmedUp(); // Roslyn 后台预热` |
| `xTestPlatform.Core/Engine/SequenceExecutor.cs` | 574 | 引擎内部 | 预热（引擎内部触发） | `await ExpressionEvaluatorFactory.EnsureWarmedUp().ConfigureAwait(false);` |
| `xTestPlatform.Core/Engine/StepHandlers/**`（约 30 个 StepHandler） | 各文件 | 引擎内部 | 执行期取值 | 均为 `_evaluator ??= ExpressionEvaluatorFactory.Default;` 模式，属于步骤执行时获取求值器，非 UI 调用 |

**结论**：预热动作（`EnsureWarmedUp`）仅由引擎内部（`RuntimeContext`、`SequenceExecutor`）触发，UI 没有主动调用预热接口；UI 侧对 `ExpressionEvaluatorFactory.Default` 的两处引用均限定在编辑期/校验期语法检查场景。

### 2.5 `SequenceLoader.Load` 直接调用

| 文件 | 行号 | 调用方 | 用途分类 | 说明 |
|---|---|---|---|---|
| `xTestPlatform.Controls/ViewModels/StepTreeControlViewModel.cs.cs` | 210 | UI 步骤树控件 | 展示 | 加载序列文件用于步骤树渲染，纯反序列化，不触发求值 |
| `ProjectEditor/ViewModels/ProjectEditorViewModel.cs` | 401 | 工程编辑器 | 展示/编辑 | 同上 |
| `xTestCopilot/Services/ToolExecutor/AiToolExecutor.Sequences.cs` | 376/433/504 | AI 工具执行器 | AI 辅助读取 | 供 AI 助手读取序列结构用于生成/校验建议，不执行序列 |
| `SequenceFileValidation/Services/Validators/SequenceCallValidator.cs` | 165 | 校验器 | 仅校验 | 校验 `Sequence_Call` 步骤引用的目标文件是否存在/可解析 |
| `xTestPlatform.Core/Engine/StepHandlers/SequenceCallStepHandler.cs` | 293/379 | 引擎内部 | 执行期加载 | 主子序列调用时加载目标序列文件，属于引擎职责 |
| `xTestPlatform.Core/Engine/RuntimeContext.cs`、`SequenceRunner.cs` | 多处 | 引擎内部 | 执行期/预热期加载 | 含 `PrecompileProjectExpressions` 内的批量加载 |

**结论**：UI 层直接调用 `SequenceLoader.Load` 均服务于“读取序列数据用于展示、校验或 AI 辅助”场景，属于只读解析行为，未触发表达式编译或设备操作，风险等级低，不属于本次 P0 重点关注的“求值/预热”耦合。

## 三、与 P0 验收标准的对照

| 设计文档要求 | 现状 |
|---|---|
| 核对所有求值入口 | 已核对：真实求值集中在 `xTestPlatform.Core` 的 `StepHandlers` 与 `RoslynScriptExecutor`，UI 无直接求值调用 |
| 核对所有预热入口 | 已核对：`EnsureWarmedUp`/`PrecompileProjectExpressions` 仅引擎内部触发 |
| 核对 UI 直接调用 | 已核对：UI 仅有校验类（`ExpressionEvaluatorFactory.Default` 用于语法检查）和展示类（`SequenceLoader.Load` 用于读取序列结构）两类间接依赖，无执行期耦合 |
| 核对插件执行与初始化依赖 | 未在本次审计范围内深入插件侧（`PluginStepHandler`/`Assembly.LoadFrom`），需单独立项审计，本清单不覆盖 |

## 四、遗留问题与建议

1. `Demo/xTestUserInterfaceDemo/...MainWindow.xaml.cs` 中仍存在 UI 直接 `new SequenceRunner(...)` 的写法，属于范围外遗留示例，建议在后续清理遗留代码时列入清单，但不影响主解决方案迁移进度。
2. 插件执行与初始化依赖（P0 另一子项）尚未审计，建议作为独立后续任务处理，避免与本清单混淆范围。
3. 本清单基于关键字检索，未做完整调用链路的动态验证；若后续要据此做代码改造，建议先针对具体改动点重新核实一次上下文。
