<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             xmlns:expr="clr-namespace:ExpressionTextBox;assembly=ExpressionTextBox"
             syncfusionskin:SfSkinManager.Theme="{syncfusionskin:SkinManagerExtension ThemeName=Windows11Light}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800"
             x:Class="VISA.UI.Views.VisaReadEditorView">
    <syncfusion:TabControlExt CloseButtonType="Hide" AllowDragDrop="False" EnableLabelEdit="False">
        <syncfusion:TabItemExt Header="VISA Read"
                               Image="pack://application:,,,/VISA.StepPlugin.UI;component/Resources/Icons/visa.png"
                               ImageHeight="20" ImageWidth="20">
            <Grid Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="连接名称:" VerticalAlignment="Center" Margin="0,6"/>
                <expr:ExpressionTextBox Grid.Row="0" Grid.Column="1" Margin="0,6"
                    ScriptText="{Binding ConnectionName, Mode=TwoWay}"
                    SequenceFile="{Binding SequenceFile, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    EditPosition="{Binding EditPosition, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="结果变量:" VerticalAlignment="Center" Margin="0,6"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="0,6"
                    Text="{Binding ResultVariable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="去除空白:" VerticalAlignment="Center" Margin="0,6"/>
                <CheckBox Grid.Row="2" Grid.Column="1" Margin="0,6" IsChecked="{Binding TrimResponse}" />
            </Grid>
        </syncfusion:TabItemExt>
    </syncfusion:TabControlExt>
</UserControl>
